<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.kbin.exp.mybatis.tenum.TEnumMapper">

  <insert id="post" parameterType="me.kbin.exp.mybatis.tenum.TEnumEntity">
    insert into TENUM_ENTITY (T_ENUM)
    VALUES (#{tEnum})
  </insert>

  <select id="get" resultType="me.kbin.exp.mybatis.tenum.TEnumEntity" parameterType="object">
    select *
    from TENUM_ENTITY
    where T_ENUM = #{tEnum}
  </select>

</mapper>